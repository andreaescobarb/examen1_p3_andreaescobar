#ifndef general_h
#define general_h

class general{
  private:
     char type;
  public:
     //constructores
     general(char);

     //getters and setters
     char getType();
     void setType(char);
     ~general();
};

#endif
