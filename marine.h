#ifndef marine_h
#define marine_h
 class marine{
   private:
      char type

    public:
      //constructores
      marine(char);

      //getters and setters
      char getType();
      void setType(char);
      ~marine();
 };

 #endif
